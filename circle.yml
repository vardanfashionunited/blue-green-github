machine:
  node:
    version: 0.10.33
dependencies:
  pre:
    - curl https://install.meteor.com/ | sh
test:
  pre:
    - git reset --hard HEAD
  override:
    # - meteor run --test
    - echo "no test"
  post:
    - docker build -t bgtest/fuww .
deployment:
  production:
    branch: master
    commands:
      - git clone https://github.com/vardanfashionunited/blue-green-github.git