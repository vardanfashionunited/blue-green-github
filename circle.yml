machine:
  services:
    - docker
  node:
    version: 0.10.33
dependencies:
  pre:
    - curl https://install.meteor.com/ | sh
  override:
    - docker info
    - docker build -t vardanfashionunited/bluegreengithub .
test:
  override:
    - cd tests && bash run_tests.sh