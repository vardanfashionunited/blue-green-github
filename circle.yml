machine:
  services:
    - docker
  node:
    version: 0.10.33
dependencies:
  pre:
    - curl https://install.meteor.com/ | sh
  override:
    - docker info
test:
  post:
    - ./test.sh
deployment:
  production:
    branch: master
    commands:
      - ./deploy_prod.sh
